# Copy to .env and fill in. Required only for the agentic investigation stage
# (scripts/investigate_email.py). The deterministic detection stage needs none
# of these.

# DeepSeek API key used by the bundled LangChain workflow.
DEEPSEEK_API_KEY=

# Optional: override the default model (deepseek-chat).
# DEEPSEEK_MODEL=deepseek-chat
