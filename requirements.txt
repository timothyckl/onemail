docker>=7.1,<8
langchain-core>=0.3,<2
langchain-openai>=1.1,<2
pydantic>=2.10,<3

# Web front-end dependencies (in addition to OneMail's own requirements.txt).
Flask>=3.0,<4
markdown>=3.5,<4

# Optional: QR-code decoding for quishing detection (the "qr" extra).
# Detection runs without these; installing them activates QR-URL recovery.
opencv-python-headless>=4.9,<5
numpy>=1.26,<3
